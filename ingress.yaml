apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: mynameisingress
spec:
  rules:
  - host: your host name
    http:
      paths:
      - path: /
        backend:
          serviceName: nginx
          servicePort: http
      - path: /v1
        backend:
          serviceName: nginx
          servicePort: http
